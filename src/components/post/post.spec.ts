import { Post } from './post';
// TODO how to pass a match paramater?

describe('app-post-dummy', () => {
  it('doesnt do a thing in the test', () => {
    expect(1).toEqual(1);
  });
});

/*
describe('app-post', () => {
  it('builds', () => {
    expect(new Post()).toBeTruthy();
  });
});
*/
